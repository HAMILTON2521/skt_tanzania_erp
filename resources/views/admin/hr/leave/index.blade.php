@extends('admin.layouts.app')

@section('page-title', 'Leave')
@section('page-subtitle', 'Capture leave applications and move them through approval states.')

@section('content')
    <div class="space-y-6">
        <nav class="flex items-center gap-3 text-sm text-slate-400"><a href="{{ route('admin.dashboard') }}" class="hover:text-white">Admin</a><span>/</span><span>People & Finance</span><span>/</span><span class="text-white">Leave</span></nav>
        @if (session('status'))<div class="rounded-2xl border border-emerald-400/20 bg-emerald-400/10 px-4 py-3 text-sm text-emerald-100">{{ session('status') }}</div>@endif
        <div class="grid gap-4 md:grid-cols-3"><div class="rounded-3xl border border-white/10 bg-slate-950/50 p-5"><p class="text-xs font-semibold uppercase tracking-[0.3em] text-cyan-300">Requests</p><p class="mt-3 text-3xl font-semibold text-white">{{ $summary['count'] }}</p></div><div class="rounded-3xl border border-white/10 bg-slate-950/50 p-5"><p class="text-xs font-semibold uppercase tracking-[0.3em] text-amber-300">Pending</p><p class="mt-3 text-3xl font-semibold text-white">{{ $summary['pending'] }}</p></div><div class="rounded-3xl border border-white/10 bg-slate-950/50 p-5"><p class="text-xs font-semibold uppercase tracking-[0.3em] text-emerald-300">Approved Days</p><p class="mt-3 text-3xl font-semibold text-white">{{ $summary['approved_days'] }}</p></div></div>
        <div class="grid gap-6 xl:grid-cols-[1fr_1.35fr]"><section class="rounded-3xl border border-white/10 bg-white/6 p-6 shadow-2xl shadow-slate-950/30 backdrop-blur"><p class="text-xs font-semibold uppercase tracking-[0.3em] text-cyan-300">Create Leave Request</p><form method="POST" action="{{ route('admin.hr.leaves.store') }}" class="mt-6 space-y-4">@csrf<div class="grid gap-4 md:grid-cols-2"><label class="block text-sm text-slate-300">Employee<select name="employee_id" class="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white" required><option value="">Select employee</option>@foreach ($employees as $employee)<option value="{{ $employee->id }}">{{ $employee->employee_code }} | {{ $employee->full_name }}</option>@endforeach</select></label><label class="block text-sm text-slate-300">Leave Type<select name="leave_type_id" class="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white" required><option value="">Select type</option>@foreach ($leaveTypes as $leaveType)<option value="{{ $leaveType->id }}">{{ $leaveType->code }} | {{ $leaveType->name }}</option>@endforeach</select></label><label class="block text-sm text-slate-300">Start Date<input type="date" name="start_date" class="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white" required></label><label class="block text-sm text-slate-300">End Date<input type="date" name="end_date" class="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white" required></label><label class="block text-sm text-slate-300">Days<input type="number" min="1" name="days_requested" class="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white" required></label><label class="block text-sm text-slate-300">Status<select name="status" class="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white">@foreach ($statuses as $value => $label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></label></div><label class="block text-sm text-slate-300">Reason<textarea name="reason" rows="4" class="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white"></textarea></label><button class="rounded-2xl border border-cyan-400/30 bg-cyan-400/10 px-5 py-3 text-sm font-medium text-cyan-100 transition hover:bg-cyan-400/20">Save leave request</button></form></section><section class="rounded-3xl border border-white/10 bg-slate-950/50 p-6 shadow-2xl shadow-slate-950/30"><form method="GET" class="grid gap-4 md:grid-cols-2"><select name="employee" class="rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white"><option value="">All employees</option>@foreach ($employees as $employee)<option value="{{ $employee->id }}" @selected($filters['employee'] == $employee->id)>{{ $employee->full_name }}</option>@endforeach</select><select name="status" class="rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white"><option value="">All statuses</option>@foreach ($statuses as $value => $label)<option value="{{ $value }}" @selected($filters['status'] === $value)>{{ $label }}</option>@endforeach</select></form><div class="mt-6 space-y-4">@forelse ($leaves as $leave)<div class="rounded-2xl border border-white/10 bg-white/5 p-4"><div class="flex flex-col gap-3 md:flex-row md:items-start md:justify-between"><div><p class="font-medium text-white">{{ $leave->employee?->full_name ?: 'Unknown employee' }}</p><p class="mt-1 text-xs text-slate-400">{{ $leave->leaveType?->name ?: 'Unclassified' }} | {{ optional($leave->start_date)->format('d M Y') }} to {{ optional($leave->end_date)->format('d M Y') }}</p><p class="mt-2 text-sm text-slate-300">{{ $leave->reason ?: 'No reason provided.' }}</p></div><div class="min-w-[220px]"><form method="POST" action="{{ route('admin.hr.leaves.update', $leave) }}" class="space-y-3">@csrf @method('PUT')<select name="status" class="w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white">@foreach ($statuses as $value => $label)<option value="{{ $value }}" @selected($leave->status === $value)>{{ $label }}</option>@endforeach</select><textarea name="rejection_reason" rows="2" placeholder="Rejection note" class="w-full rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-white">{{ $leave->rejection_reason }}</textarea><button class="w-full rounded-2xl border border-cyan-400/30 bg-cyan-400/10 px-4 py-3 text-sm font-medium text-cyan-100 transition hover:bg-cyan-400/20">Update status</button></form></div></div></div>@empty<div class="rounded-2xl border border-white/10 bg-white/5 px-4 py-10 text-center text-slate-400">No leave requests found.</div>@endforelse</div><div class="mt-4">{{ $leaves->links() }}</div></section></div>
    </div>
@endsection
